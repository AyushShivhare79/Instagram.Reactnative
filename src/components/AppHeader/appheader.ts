import { COLORS } from '@/theme/color/color';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  headerBackground: { backgroundColor: COLORS.transparent },
  headerText: { color: COLORS.black },
});
